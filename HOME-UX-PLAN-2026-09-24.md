# 首页工具「零思考」使用体验改造方案

**对象**：`gifcompressors.com` 首页 `#gif-tool`（`site/src/components/GifTool.astro` + `site/public/js/gif-compressor.js`，同一组件被 10 个工具页复用）
**日期**：2026-09-24
**目标**：让用户不需要理解任何概念就能拿到想要的结果 —— 从"参数控制台"改成"扔进去就完事"。
**本文只做方案，不改代码。**

> 下文中所有标 **实测** 的结论，均来自对线上站点真实点击「Try this GIF」跑完整流程后取回的 DOM/时序数据，不是推测。

---

## 一、当前链路实测还原

```
落地 → 看 H1 → 看演示卡 → 找到投放区 → 选文件
     → 【5.0 秒空白】→ 面板突然出现（10 个控件）
     → 用户判断该动哪个 → 点预设（可能毫无反应 / 反而变大）
     → 点下载 → 无成功反馈 → 离开
```

| 实测项 | 结果 |
|---|---|
| 点「Try this GIF」→ 工具面板可见 | **4,955 ms** |
| → 下载按钮可用 | **5,964 ms** |
| 面板内同时可见的交互控件 | 模式×3 + 滑块×1 + 预设×4 + 对比滑块×1 + 重置 + 下载 = **约 11 个** |
| 高亮的模式按钮 | `quality` |
| JS 实际生效的模式 | `balanced`（`gif-compressor.js:51`） |
| `#batch-toggle` 是否存在 | **不存在（MISSING）** |
| 投放区文档位置（桌面 1145px 视口） | y=412–783，「Select File」按钮 y=**648–688** |
| 页面总高 | 4,568 px（工具之后还有约 3,800 px 说明性内容） |

---

## 二、五个"逼用户思考"的断点（按严重度排序）

### 断点 1 · 面板上显示的档位是假的 —— 高亮 Quality，实际跑 Balanced 【实测】

`GifTool.astro:108` 把 `Quality` 硬编码成选中态（`bg-accent text-white`），而 `gif-compressor.js:51` 的初始值是 `let mode = 'balanced'`，且 `init()` 从未同步按钮状态。

**后果**：用户看到 Quality 亮着 → 认为"现在是保质量档" → 拿到一个实际按 Balanced 压出来的结果 → 若不满意，他会去调滑块，因为他相信档位没问题。**起点就是错的，后面所有决策都建立在错误信息上。**

同类问题：`bd-color` 显示 `gifsicle`（不是数值）、`bd-lzw` 显示 `75`（只是把滑块值回显）、`bd-frames` 显示 `-98%`（实测，其实是总压缩率被复制过来的，并没有丢 98% 帧）。而首页 `index.astro:163` 明确写着"用 Breakdown 面板看你的节省到底来自哪里"。**一个承诺给技术拆解、实际填充占位垃圾的面板，比没有这个面板更糟。**

### 断点 2 · 平台预设会静默失效，甚至让文件变大 —— 【实测】

```
点「< 8MB」（当前文件 1.2MB）：
  level 75 → 75、体积 24KB → 24KB，完全没动
  但按钮边框变成选中态（border-accent）→ 看起来"生效了"
  无 toast、无进度文字、无任何说明

点「< 500KB」：
  level 75 → 58、体积 24 KB → 25 KB —— 文件反而变大了
```

两个根因：
1. `gif-compressor.js:229` `if (origSize <= p.maxSize) return;` —— 已达标的文件直接静默返回，但调用方已经把按钮点亮了（`:240`），**制造了"操作成功"的假象**。
2. 预设换算公式 `level = round((1 - target/orig) * 100)`（`:231`）方向性错误：任何本来就远低于目标的文件，算出的 level 都低于默认 75，于是压缩强度下降、产物变大。**"帮你压到 500KB 以内"的按钮，产出了一个比现状更大的文件，并且不告知。**

### 断点 3 · 加载后有约 5 秒的"死区" 【实测】

`loadFile()` 在显示面板之前，串行 await 了 `loadEngine()`（首次访问需从 `esm.sh` 拉约 200KB WASM）+ `parseGIF` + `decompressFrames`（全帧解压做分析）。面板 `tool-panel` 直到 `:377` 才解除 hidden。

- 这 5 秒里用户只看到一条 1.5px 的进度条和"Reading file..."，**画面没有任何东西在动**。
- 分析（帧数/颜色数）是纯展示性信息，不是压缩的前置条件，却被放在了关键路径上。
- 若 `esm.sh` 被墙或失败，直接掉进 `:181` 的 `Compression failed` toast，工具彻底不可用，用户不知道是自己文件的问题还是站点的问题。

对工具站来说，**首屏 5 秒无反馈足以让相当比例的用户认为页面坏了并关掉**。

### 断点 4 · Batch 模式：被三处文案和机器可读 schema 承诺，但 UI 完全不存在 【实测】

- `index.astro:50` 的 FAQPage schema 原文："Toggle **Batch mode** in the tool to compress multiple GIFs simultaneously"
- `index.astro:341` 页面 FAQ 正文同样承诺
- `/compress-gif/` `:165`："Our batch mode lets you compress up to **20 GIFs** simultaneously"
- `/about/` 两处把它当作自研卖点

而 `#batch-toggle` / `#batch-section` / `#batch-list` 在任何 `.astro` 里都不存在（`grep src/` 无命中，实测返回 MISSING），`batch-compressor.js` 因 `if (batchToggle)` 守卫整体不执行。

**用户旅程**：读 FAQ → 在工具里找一个叫 Batch 的开关 → 找不到 → 怀疑自己 → 放弃。这是"需要思考"最典型的成因：**界面没有回答文案提出的问题。**

（附带：`batch-compressor.js:48` 取 `#drop-zone` 后未判空即 `addEventListener`，在嵌了 GifTool 但无投放区的页面上会抛未捕获异常。目前所有页面都有投放区所以未暴露，属隐患。）

### 断点 5 · 输入通道脆弱，失败一律沉默

| 场景 | 现状 | 后果 |
|---|---|---|
| 把文件拖到投放区以外（页面任意空白、导航、正文） | 只有 `#drop-zone` 绑了 `dragover/drop`，无 window 级 `preventDefault` | **浏览器直接跳转打开该文件，当前会话整体丢失** |
| 文件加载后再拖第二个 GIF | 投放区已被加 `hidden`（`:376`），drop 监听随元素消失失效 | 换图必须先去点一个灰色小字"Compress another GIF" |
| 粘贴 | `if (item.type === 'image/gif')`（`:269`） | 从图片右键复制、从 Discord 复制常报为 `image/png` 或纯 URL 文本 → **静默无反应**；截图粘贴同理 |
| 拖入 JPG/PNG | `accept` 只约束文件选择器，**对拖放无效** | 喂给 gifsicle，最坏得到一次失败 toast |
| 手机端点投放区边缘 | 整个卡片 `cursor-pointer` 且绑 `click→fileInput.click()`，只有 URL 那一行 `stopPropagation` | 误触直接弹系统文件选择器 |

### 断点 6 · 首屏看不到操作入口

实测投放区在桌面 1145px 视口下位于 y=412–783，「Select File」按钮 y=648–688 —— 桌面尚可。但演示卡在 `<640px` 下从横排转为 `flex-col` 堆叠，图片块由 `sm:w-[220px]` 变成满宽（360px 视口下约 246px 高），**卡片高度由实测 177px 涨到约 420px**，叠加后投放区起点推到约 650px，在 360×640 主流机型上**整块落到折叠线以下**。

（此条为按断点规则的推算，需 DevTools 360×640 实测复核。而移动端平均排名 19.75 远优于桌面 78.34，意味着移动端流量占比将继续上升，这条的权重要往上提。）

---

## 三、方案

### 设计主张

> **默认值即答案。控件不是功能，是负担。**

当前设计把"我能调什么"摆在用户面前（11 个控件）；目标设计只回答"你要干什么"。用户真实的意图从来不是"我要把 level 设成 58"，而是"我要发到 Discord / 塞进邮件 / 放网页上"。

### L0 · 先止血：删除一切欺骗性状态（工作量 0.5 天，最高优先）

| # | 动作 | 对应断点 |
|---|---|---|
| L0-1 | 初始化时用 JS 依据 `mode`/`level` 真值渲染按钮选中态，删除模板里硬编码的 `bg-accent` | 1 |
| L0-2 | Breakdown 面板：**填入真实的 gifsicle 拆解数据**（颜色数变化、帧增量优化字节数、LZW/lossy 各自节省量），或整体隐藏。禁止占位假数据 | 1 |
| L0-3 | 预设点击后：若原始文件已达标，不点亮按钮，改为显示明确结论 ——「你的 GIF 已经小于 8MB，无需再压」+ 当前体积 | 2 |
| L0-4 | 修预设方向性缺陷：`level` 只允许**上调**（`Math.max(currentLevel, computed)`）；若达标即走 L0-3 分支。任何情况下产物体积不得大于当前默认结果，除非用户显式要求 | 2 |
| L0-5 | Batch：**要么补 UI，要么全站点删除该承诺**（含 FAQPage schema、`/compress-gif/`、`/about/`、`/gif-to-mp4/` 的对应句）。不许留着 | 4 |
| L0-6 | `batch-compressor.js` 取 DOM 后判空再绑事件 | 4 |
| L0-7 | 演示卡数字统一：卡面写 27 KB、`demo-home-v3.gif` 实际 21.3 KB、跑完实测得 24 KB —— 三处对齐成同一真实值 | 1 |

### L1 · 零决策主路径（1 周）

**L1-1 把「选文件」变成唯一的动作**
投放后不需要第二次点击。当前已有 `applyCompression()` 自动跑（好），但要让它**立刻可见**：
- 拆成两段：① 拿到 File → 同步创建 objectURL → **马上显示面板 + 原图预览 + "正在压缩…"骨架屏**；② 引擎加载与全帧分析全部改成后台异步，完成后填充。
- 目标：点选到看到面板 **< 300ms**（现在 4,955ms），压缩完成时间保持但发生在"已经能看到结果"之后。
- 引擎预热：`requestIdleCallback` 里预取 WASM（不抢 LCP 带宽），或对演示 GIF 走已缓存路径。
- 引擎兜底：`esm.sh` 失败时改尝试本地 `/vendor/` 副本，并给出人话错误（"压缩引擎加载失败，请刷新重试"），而不是 `Compression failed`。

**L1-2 用一个"你要发到哪?"取代 Mode + Level + 预设三组控件**

```
这个 GIF 你要用在哪？
[ Discord ]  [ X / Twitter ]  [ 邮件 ]  [ 网页 ]  [ 不知道，越小越好 ]
```
- 每个按钮文案带平台名 + 限额（现在只有 `< 8MB`，平台名藏在 `title` 里，**移动端完全不可见**，实测已确认）。
- 选完直接出结论横幅：「✅ Discord 免费版上限 8MB · 你的是 24KB · 可以直接用」。**把"达标判断"这个思考替用户做完。**
- 默认不选 = 当前的 balanced/75，仍然自动出结果。也就是：**什么都不点也已经能拿到好东西。**
- 「不知道，越小越好」= 新增，最符合无脑使用心智，也最可能是最高频选项。

**L1-3 把收益写进主按钮，用户不必读任何面板**
```
现在：  [⬇ Download Compressed GIF]          （实测按钮文案始终不变，无数字）
改为：  [⬇  Download · 1.2 MB → 24 KB (−98%)]
```
这是"零思考"最关键的一处：**唯一的决策信息（值不值得下载）出现在唯一的下一步动作上**。

**L1-4 高级控件收进「高级设置」折叠区**
- Mode 三档 / Level 滑块 / Breakdown 收进 `<details>`，默认收起。
- 同时把这批控件的完整版交给 `/gif-optimizer/`（该页定位就是"Advanced Per-Parameter Compression"）。
- **顺带收益**：这同时解决了 GSC 分析里的自我蚕食 —— 首页不再长得像一个高级参数工具，与 `/gif-optimizer/` 的意图边界随之分开。两份方案在这里是同一个动作。

**L1-5 滑块语义化（若保留）**
- `Level 75` 这种裸数字必须去掉方向歧义：改成两端标签「画质 ↔ 体积」，或干脆换成 3 档具名选择（视觉无损 / 均衡 / 极限压缩）。
- 滑块拖动过程中给实时预估，别让用户拖完等 600ms 防抖再看结果。

### L2 · 输入通道兜底（2–3 天）

| # | 动作 |
|---|---|
| L2-1 | **window 级 `dragover`/`drop` + `preventDefault`**，投放落点自动滚动并高亮工具卡。这条单独就能避免一批次会话被浏览器劫持掉 |
| L2-2 | 结果态保留全局投放：加载后再拖一个新 GIF，直接替换当前文件（现在必须点 reset） |
| L2-3 | 粘贴放宽到 `kind === 'file'` 且 `type.startsWith('image/')` 或文件名以 `.gif` 结尾；非 GIF 走转换引导；纯 URL 文本自动填进 URL 框 |
| L2-4 | 非 GIF 文件给出具体下一步（"这是 JPG，要不要转成 GIF / 直接压缩？"），而不是让 gifsicle 报错 |
| L2-5 | 手机端把「Select File」抬进首屏：压缩演示卡（把图片缩到 ~120px 或改成右侧小图常驻）、或首屏直接放投放区、演示卡下移 |
| L2-6 | 收窄点击热区：`drop-zone` 的整卡 click 改成只由图标+标题+按钮触发，避免误开系统文件选择器 |

### L3 · 完成感与信心（2 天）

- **L3-1 下载成功反馈**：点完下载给"已保存为 `xxx-compressed.gif`"，并附二次动作（复制到剪贴板 / 发到某平台）。手机端文件静默落进下载目录，不给回执 = 用户不知道成没成。
- **L3-2 对比滑块按 GIF 真实宽高比设 `aspect-ratio`**：现在被 `aspect-ratio: 1`（`GifTool.astro:58`）强制成正方，横幅 GIF 严重留白，看不出画质差异，只能靠信任。看不出差异 → 不敢下载 → 必须"自己思考"。
- **L3-3 `#usage-counter` 接真实数据**（实测常态显示 `0 GIFs compressed`）。"0" 是反信号，比不显示更糟。
- **L3-4 首屏信息层级降权**：`Built by web performance engineers · Last updated: August 2026` 目前压在副标题上方（`index.astro:76`），对"我只想把这张图压小"的用户是纯噪声，移到工具下方（E-E-A-T 信号不丢，Google 不要求位置）。

---

## 四、目标形态：一条不需要思考的路径

```
① 拖进来（页面任意位置都行）
② < 300ms  立刻看到原图 + 正在压缩
③ ~1.5s    按钮变成 [⬇ Download · 1.2 MB → 24 KB (−98%)]
④ 想省事？ 点按钮 → 完
   有去处？ 点 [Discord] → 「✅ 24KB，远低于 8MB 上限，直接用」→ 点按钮
⑤ 想调？   「高级设置」在这里（默认收起）
```
**全程 0 个必须理解的概念。** 不需要知道 level、lossy、LZW、Quality/Balanced/Max 的区别、也不需要知道 8MB 是谁的上限。

---

## 五、怎么验证有效

| 指标 | 现状 | 目标 | 取数方式 |
|---|---|---|---|
| 选文件 → 面板可见 | **4,955 ms（实测）** | < 300 ms | Performance mark，或脚本量测 |
| 选文件 → 下载按钮可用 | **5,964 ms（实测）** | < 2,500 ms | 同上 |
| 首屏内可见投放区（360×640） | 待实测（推算为否） | 是 | DevTools |
| 完成一次下载的会话占比 | 无埋点 | 建立基线后 +50% | GA4 事件：`file_loaded` / `preset_used` / `download_clicked` |
| 预设点击后无变化的会话 | 存在（实测静默失效） | 0 | `preset_noop` 事件 |
| 因拖到卡外导致的跳出 | 存在 | 0 | `window` drop 事件计数 |
| 移动端平均排名 | 19.75 | 维持 ≤25 | GSC 分设备 |

GA 已在 `Base.astro` 装好（G-N2KTR7FRKD），补 4–6 个自定义事件即可，不需要新工具。

---

## 六、明确不建议做的事

- **不做**引导蒙层 / 新功能 tour / "第一步第二步"气泡。需要教用户用，就说明设计没做完 —— 与"无脑使用"目标直接冲突。
- **不做**注册、登录、历史记录、云端保存。"不用上传"是本站唯一真实差异点，任何账号化都会摧毁它。
- **不新增**首页控件数量。本方案净效果是把首页可见控件从 ~11 个降到 1 主按钮 + 5 个场景按钮，**做减法**。
- **不做**实时逐帧画质对比、逐帧 diff 视图等炫技功能。目标用户要的是"够小 + 看起来没坏"，不是逐帧审查。
- **不要**把 Mode 三档直接删掉 —— 开发者/性能向用户（也是外链和内容资产的目标受众）确实需要，收进折叠区而非移除。

---

## 附：证据索引

| 结论 | 代码位置 |
|---|---|
| 高亮 Quality / 实际 balanced | `site/src/components/GifTool.astro:108` · `site/public/js/gif-compressor.js:51` · `init()` `:386` |
| 预设静默失效 | `gif-compressor.js:229`（early return）+ `:240`（点亮按钮） |
| 预设方向性缺陷（产物变大） | `gif-compressor.js:231` level 换算公式 |
| Breakdown 假数据 | `gif-compressor.js:158-160` · 面板 `GifTool.astro:136-141` · 承诺文案 `index.astro:163` |
| Batch 承诺无实现 | `index.astro:50,341` · `compress-gif.astro:165` · `about.astro:44,97` · `batch-compressor.js:14-24` |
| 5 秒死区根因 | `gif-compressor.js:356-358`（await 引擎+全帧解析）先于 `:377`（解除面板 hidden） |
| 引擎单点依赖 esm.sh | `gif-compressor.js:8-9` |
| 无 window 级拖放兜底 | `gif-compressor.js:252-263`（仅 `#drop-zone`） |
| 加载后无法再拖入 | `gif-compressor.js:376`（投放区加 hidden） |
| 粘贴仅认 `image/gif` | `gif-compressor.js:269` |
| 对比区强制正方 | `GifTool.astro:58` `aspect-ratio: 1` |
| 演示卡数字不一致 | `index.astro:100-102`（27 KB）· `public/demo/demo-home-v3.gif`（21,769 B）· 实测产物 24 KB |
